# Travel Diary MVP - 백엔드 의존성
# 2번(백엔드·통합) 담당자가 관리합니다.

# API 서버
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9   # 사진 다중 업로드(멀티파트) 처리

# 설정 로딩
python-dotenv==1.0.1

# 사진 분석·선별 (4번)
Pillow==10.3.0            # EXIF 추출, 이미지 정규화
ImageHash==4.3.1          # 유사사진 perceptual hash

# 테스트
pytest==8.2.2
httpx==0.27.0             # FastAPI 테스트 클라이언트

# AI 문구 생성 (5번) - 서버 측에서만 사용
# anthropic==0.34.0       # 사용할 AI SDK 확정 후 주석 해제
