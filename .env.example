# 실제 값을 채운 뒤 .env 로 복사해서 사용하세요. .env 는 커밋하지 않습니다.
# cp .env.example .env

# 지도 (프런트에 주입되는 공개 토큰. 허용 URL/권한 범위를 제한하세요)
MAPBOX_ACCESS_TOKEN=pk.여기에_공개_토큰

# AI 문구 생성용 키 (서버에서만 사용, 브라우저에 넣지 마세요)
AI_API_KEY=여기에_비밀_키

# 저장 경로
UPLOAD_DIR=uploads
OUTPUT_DIR=outputs

# 업로드 제한
MAX_PHOTOS=30
MAX_PHOTO_MB=15
